# Codebase to hold all the workflows and snippets of GCP Bigquery service #

1.  Small snippets in python - Bigquery service
2.  Make sure to provide the correct inputs
        A.  util.py is to get the service key token for authentication. Provide the correct path where the key is stored
        B.  For extract methods provide the corrct Bigquery dataset ID and GCS sink path location with file extension
        C.  Add the path for sink data result json 